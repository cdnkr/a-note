{
  "name": "a-note-extension",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first webpage annotation Chrome extension.",
  "scripts": {
    "build": "vite build",
    "build:watch": "vite build --watch",
    "release": "vite build && mkdir -p release && rm -f \"release/${npm_package_name}-${npm_package_version}.zip\" && cd dist && zip -qr \"../release/${npm_package_name}-${npm_package_version}.zip\" . -x '*.DS_Store'",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
